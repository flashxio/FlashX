Convert a FlashMatrixR vector to a FlashMatrixR factor vector.

Description:

     Convert a FlashMatrixR vector to a FlashMatrixR factor vector.

Usage:

     fm.as.factor(fm, num.levels = -1)
     
Arguments:

      fm: a FlashMatrixR vector.

Value:

     a FlashMatrixR factor vector.

