Convert a FlashMatrixR matrix to a FlashMatrixR vector.

Description:

     The matrix must have only one row or one column. Otherwise, the
     function returns an error.

Usage:

     fm.as.vector(fm)
     
Arguments:

      fm: a FlashMatrixR matrix

Value:

     a FlashMatrixR vector

